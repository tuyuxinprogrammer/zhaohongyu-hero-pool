# 赵宏宇专属奖池 — 部署说明

## 📁 项目结构

```
赵宏宇专属/
├── index.html          ← 主页面（唯一需要的HTML文件）
├── hero_img/           ← 英雄图片文件夹
│   ├── .gitkeep
│   ├── 李白.png        ← 示例：英雄名.png
│   ├── 韩信.jpg        ← 支持 jpg
│   ├── 鲁班七号.webp   ← 支持 webp
│   └── ...             ← 按英雄名命名即可
└── 部署说明.md          ← 本文件
```

> **图片命名规则**：文件名 = 英雄名称（不含后缀），程序自动匹配 png / jpg / jpeg / webp 格式。

---

## 🚀 方式一：GitHub Pages 部署（推荐，外网可访问）

### 步骤 1：创建 GitHub 仓库

1. 打开 [GitHub](https://github.com) 并登录
2. 点击右上角 **+** → **New repository**
3. 仓库名填写（例如）：`zhaohongyu-hero-pool`
4. 选择 **Public**（公开）
5. 勾选 **Add a README file**（可选，后面会覆盖）
6. 点击 **Create repository**

### 步骤 2：上传项目文件

**方法 A — 网页直接上传（最简单）：**

1. 进入刚创建的仓库页面
2. 点击 **Add file** → **Upload files**
3. 将 `index.html` 和 `hero_img` 文件夹**一起拖入**浏览器
4. 在 Commit message 里写 `初始化英雄奖池`
5. 点击 **Commit changes**

**方法 B — Git 命令行：**

```bash
cd "D:\赵宏宇专属"
git init
git add index.html hero_img/
git commit -m "初始化英雄奖池"
git branch -M main
git remote add origin https://github.com/你的用户名/zhaohongyu-hero-pool.git
git push -u origin main
```

### 步骤 3：启用 GitHub Pages

1. 进入仓库 → **Settings**（顶部标签）
2. 左侧菜单点击 **Pages**（在 "Code and automation" 下）
3. **Source** 选择 **Deploy from a branch**
4. **Branch** 选择 `main`，文件夹选 **/ (root)**，点击 **Save**
5. 等待 1-2 分钟，页面顶部会显示：
   > ✅ Your site is live at `https://你的用户名.github.io/zhaohongyu-hero-pool/`

### 步骤 4：访问

- 用手机、电脑、平板浏览器打开上面的链接即可
- 各设备之间的数据互不干扰（数据存储在各自浏览器的 localStorage 中）

---

## 🏠 方式二：本地离线使用

### 直接双击打开

在文件资源管理器中找到 `index.html`，**双击**即可在浏览器中打开。

> ⚠️ 注意：由于浏览器安全策略，`file://` 协议下图片探测（Image() onerror）可能受限。如果图片不显示，建议用下面的本地服务器方式。

### 本地简易服务器（推荐）

在项目目录下运行以下任一命令：

```bash
# Python 3（Windows 自带或需安装）
cd "D:\赵宏宇专属"
python -m http.server 8080

# Node.js（需安装 npx）
cd "D:\赵宏宇专属"
npx serve .

# VS Code Live Server 插件
# 右键 index.html → "Open with Live Server"
```

然后浏览器访问 `http://localhost:8080`

---

## 📱 局域网多设备访问

1. 确保电脑和手机/平板在同一 WiFi 下
2. 电脑上启动本地服务器（如 `python -m http.server 8080`）
3. 查看电脑的局域网 IP 地址：
   - Windows：打开 cmd，输入 `ipconfig`，找到 `IPv4 地址`（如 `192.168.1.105`）
4. 手机/平板浏览器访问：`http://192.168.1.105:8080`

> 此时各设备仍独立存储数据（localStorage 按设备隔离）。

---

## 🖼️ 添加英雄图片

1. 将图片文件放入 `hero_img/` 文件夹
2. **文件名必须和你在网页中输入的名称完全一致**（不含扩展名）
3. 例如：奖池中英雄名为"李白"，图片文件命名为 `李白.png` 或 `李白.jpg` 等
4. 添加图片后刷新页面，已存在的英雄头像会自动加载
5. 如果没有对应图片，英雄将以默认头像占位符显示

---

## 💾 数据说明

- 所有奖池数据存储在浏览器的 **localStorage** 中
- 清除浏览器数据会导致奖池配置丢失
- 不同浏览器/设备之间的数据互相独立
- 可以通过浏览器开发者工具（F12 → Application → Local Storage）手动备份数据

---

## 🎨 技术栈

- 纯 HTML + CSS + JavaScript（零依赖，无框架）
- 深色电竞主题 + 金色高光
- 移动端响应式适配
- localStorage 持久化存储
- 图片格式自动探测（png/jpg/jpeg/webp）
